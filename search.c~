#include <stdio.h>
#include "_printf.h"
#include <stdarg.h>
/**
 *struct format - a new type struct car
 *
 */
search(const char fmt, int *c, va_list *list)
{
	/**
	  *Define an array of structs
	  **/
	while (structarray)
	{
		format[i];
	}
}
