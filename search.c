#include <stdio.h>
#include "_printf.h"
#include <stdarg.h>
/**
 *search - 
 *format - a new type struct car
 *c
 */
search(const char format, int *c, va_list *list)
{
	/**
	  *Define an array of structs
	  **/
	while (structarray)
	{
		format[i];
	}
}
